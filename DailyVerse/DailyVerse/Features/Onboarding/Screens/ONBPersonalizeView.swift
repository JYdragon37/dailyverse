import SwiftUI

// 레거시 — 새 온보딩(4화면)에서 제거됨
// 보존: onboarding/legacy-backup 브랜치 참고
struct ONBPersonalizeView: View {
    @ObservedObject var vm: OnboardingViewModel
    var body: some View { EmptyView() }
}
